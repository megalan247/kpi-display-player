style.
  html,body,webview {
    margin: 0;
    padding: 0;
    width:100%;
    height: 100%;
  }

  .half {
      width: 50%;
      display: inline-flex;
  }

.half
    webview(src=main[0].site_url)
.half
    webview(src=main[1].site_url)